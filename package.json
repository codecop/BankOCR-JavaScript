{
    "name": "bank-ocr-refactoring-kata",
    "version": "0.0.1",
    "scripts": {
        "test": "jest",
        "test:coverage": "jest --coverage",
        "test:watch": "jest --watchAll"
    },
    "devDependencies": {
        "@types/jest": "^27.0.0",
        "jest": "^27.0.0"
    }
}
